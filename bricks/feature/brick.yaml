name: feature
description: Brick to build a clean architecture feature
version: 0.1.0+1

vars:
  feature:
    type: string
    description: Feature name
    default: User
    prompt: What is your Feature name? ex auth/user/cards
  # stateful:
  #   type: boolean
  #   description: teste
  #   default: false
  #   prompt: Its a statefull?
# mason make page -o ./generated/page
# -c ./bricks/page/config.json
# mason make feature -o ./generated/example_repo construir na raiz para ter acesso a pasta de test tambem

#nome da feature
#enum para tipo de metodo http
#se tem input no usecase/data
#se tem query parameter

# mason make feature -o ./

# mason add -g todos --git-url https://github.com/felangel/mason --git-path bricks/todos
# list all locally installed bricks
# mason list

# list all globally installed bricks
# mason list --global

# use alias "ls" instead of "list" for a shorthand syntax
# mason ls

# list all globally installed bricks shorthand syntax
# mason ls -g
